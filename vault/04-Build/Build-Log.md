---
tags: [build, log, vitish-2026, shm]
created: 2026-08-13
---

# Build Log

Running log of the 36-hour build. Newest entries at TOP. One row per significant work block; keep it terse — the git history is the detailed record.

## Template

| Date/time | Task | Status | Next |
|---|---|---|---|
| 2026-08-14 | **D2-12 (2-day plan) — seeded-defect demo grounded in Z24/S101**: the damage story is now a **named, physically-grounded EI seed**, not a forced 4 Hz tonal. `models/vibration/seeded_defect.py` catalogues the real benchmark defects (Z24 progressive pier-settlement → mid-span cracking → **tendon rupture**; S101 girder saw-cut catalogued but not in the demo arc), each a per-span-zone EI-loss fraction. The demo injector stages the Z24 sequence IN CAMPAIGN ORDER as the storyboard cross-fades (settlement −0.6% → +cracking → +tendon full **f1 3.80 → 3.24 Hz, main-span EI −22.6%**), and the accel stream carries a **modal resonance at the seeded f1** — the *measured* frequency shifts per the evidence, no magic. New `GET /api/bridge/z24/seeded-defect` (label, source, active[], per-span EI%, f1 drift, honest note). `backend/app/stiffness.py` tracking gate widened 0.08 → **0.20** so the measured overlay FOLLOWS the −15% slide (was freezing at −8% and contradicting the narrative) while still rejecting non-modal bursts; twin `StoryControls` hint + `ProvenancePanel` show the seeded label/EI/f1; overlay clears honestly on healthy ("SYSTEM NOMINAL"). New gate 8 `test_seeded_defect.py` 49/49. **Gates: all 8 PASS, 291 checks (85+26+15+31+15+36+34+49), exit 0**; live-verified: healthy baseline locks at f1_ref 3.89 → rupture slides measured f1 to 3.23 (agrees with seeded 3.244) → BHI 87→AMBER→RED 33.9; headless-Chromium verified the seeded hint + provenance block render and clear on healthy | ✅ done | Finish phase: full demo dry-run + YC polish + push |
| 2026-08-14 | **D2-7 (2-day plan) — Cesium georeferenced layer**: the twin now has a real-map context view at the Z24 reference site (Nottwil, CH, A1 — lat 47.135, lng 8.165). "Geo view" button swaps the R3F engineering view for a live Cesium ion globe: **real World Terrain + real Google Photorealistic 3D Tiles** (ion assets 1 + 2 + 2275207) with the modeled Z24 14+30+14 m box girder georeferenced over the site (58 m box colored by live BHI state, heading aligned to the real A1 crossing) and deck sensor nodes n6/n7/n8 at x=−10/0/+10 m. Honest by construction — caption chips read "terrain & 3D tiles: **real** (Cesium ion / Google) · Z24 structure: **model** · not to scale" and the box colour tracks live BHI (RED at arc end). Cesium is dynamically imported (`vite-plugin-cesium`, ~30 MB chunk loads only when Geo view opens — initial bundle unchanged); token lives in gitignored `twin/.env` (`VITE_CESIUM_TOKEN`), NEVER committed — teammates copy it from the owner. Graceful fallback card if token missing / tiles unreachable ("Use the 3D engineering view"). **Gates: all 242 still PASS; twin `npm run build` clean**; verified in real headless Chromium: Geo view mounts (cesium-widget canvas 606×798), no fallback, ion assets 1/2/2275207 + Google `.glb` 3D-tile geometry streaming, screenshot pixel-analysis confirms drawn terrain (luma 12→255, 66.6% non-dark), only console output = expected THREE.Clock deprecation + R3F teardown warning on view switch | ✅ done | D2-12 seeded-defect demo grounded in Z24/S101 |
| 2026-08-14 | **D2-11 (2-day plan) — Markov + Bayesian-updating condition UI**: the twin now draws the LTBP Markov projection that **re-anchors on every poll**: the live BHI (which the measured crack state moves) re-maps to a current NBI condition, and the empirical fleet prior fans out a yearly expected / p10–p90 uncertainty band from there. New `GET /api/bridge/z24/deterioration` poller (`twin/src/lib/deterioration.ts`, 5 s) → store `DeteriorationState` → `twin/src/panels/DeteriorationPanel.tsx` renders current-BHI / NBI-condition / next-inspection chips + Recharts curve (expected line, dashed p10/p90, grey uncertainty fan, red NBI-4 reference, amber "inspect yr N" trigger line at first year P(NBI≤4)≥25%). Honest framing on-screen: `priorsLabel` = "empirical LTBP prior, small n (44 FHWA InfoBridge pilot bridges, 1993–2025)" and the `note` ("probabilistic model… not a certified RUL"). Fix caught in verification: recharts v3 `ResponsiveContainer` latched width 0 when the chart mounted in a non-composited context → added `initialDimension` so the curve paints immediately and corrects on first real ResizeObserver measurement. **Gates: all 242 PASS; twin `npm run build` clean**; verified in a real headless Chromium (hidden preview pane reports 0×0): chart renders 9 curve paths + 3 uncertainty-area layers + NBI-4 + inspect reference lines; det-stats live (BHI 34.x RED → NBI 4.0, next inspect yr 1) | ✅ done | D2-7 Cesium georeferenced layer |
| 2026-08-14 | **D2-8 + D2-9 (2-day plan) — simulated-clock label + visualization realism bundle**: (a) `SimClockBadge` in the HUD header anchors every temporal claim to the simulated monitoring clock — "SIM day N/365 · ×2 d/s time-lapse · f1-ref X Hz · Y°C sim" (reads the backend sim_clock, never wall-clock; the Z24 campaign ran 11 Nov 1998 → 20 Aug 1999). (b) `SceneOverlay` over the 3D view: damage heat-map legend with PHYSICAL UNITS ("EI stiffness loss · main span — 0%→10%→20%→35%+, model-inferred", the ramp saturating where the deck tint saturates), labeled deflection exaggeration ("deformation & mode flex exaggerated for visibility — not to scale"), and the stale-sensor legend. (c) **Stale-sensor glyphs (GREY)**: store now tracks per-node last-seen (`nodeSeen`, set by WS accel ingest + offline replay); a node that reported and then went quiet >4 s renders GREY instead of inventing a health colour. (d) **Uncertainty band**: BHI trend now draws the ±u envelope behind the line ("shaded = ±u"). **Gates: backend 242 all PASS, twin `npm run build` clean**; browser-verified LIVE: BHI 33.8 RED at arc endpoint, honest "VIBRATION ANOMALY · forced 4 Hz" hint | ✅ done | D2-7 Cesium georeferenced layer |
| 2026-08-14 | **D1-6 (2-day plan) — digital-shadow label + provenance UI**: the twin now answers "what am I actually looking at?" on screen. New `twin/src/panels/ProvenancePanel.tsx` renders the D1-5 manifest straight from `GET /api/manifest` (never invented in the frontend): a prominent **DIGITAL SHADOW · one-way data · model never commands the bridge** chip, the data-source label (real Z24 replay / modeled synthetic / third-party live feed / offline), a per-channel real-vs-modeled list (nodes 6/7/8 = real replay), the backend honesty note, the live-feed line (bridge=live-demo, never fused into Z24 BHI), and the D2-10 sim-clock + modeled-temperature provenance line (day N/365 time-lapse, "modeled"). Store gains `manifest` state + thermal-overlay fields; `lib/manifest.ts` polls the manifest every 5 s with an honest offline default ("replay fixtures (backend unreachable)"). **Fix caught live**: after the demo driver holds at RED, a "healthy" reset left `cv`/`load` stale at 0.55/0.40 → bridge stalled at AMBER instead of recovering → `fusion.on_control` now resets cv→default + load→default on `scenario: healthy` (simulator already relaxes vib). **Arc gate 11→15** with a recovery regression (healthy resets cv/load; feed healthy → GREEN back in [75,90]). **Gates 242 total** (85+26+15+31+15+36+34), all PASS; browser-verified LIVE: provenance panel + sim clock advancing + recovery GREEN | ✅ done | D2-8 simulated clock / time-lapse label in the HUD |
| 2026-08-14 | **D1-5 (2-day plan) — data-realism manifest + synthetic channel models**: every number the twin shows now has an honest source label. `backend/app/channel_models.py` = per-channel provenance (real Z24 replay | modeled synthetic | third-party live-demo), the documented synthetic measurement chain (anti-alias lowpass 45 Hz → bias drift → transient spikes → 12-bit ADC @ 4.0 m/s² VREF) which the synthetic stream **actually applies** (deterministic per node/seed, never aspirational), and `build_manifest()` answering "what am I looking at": data_source, channels, synthetic_model spec, honesty (real vs modeled channels + plain-language note), live_public_feed (bridge=live-demo, "never fused into z24 BHI"), datasets (Z24 mirror). Simulator registers whichever source it uses (registry drives the manifest). New `GET /api/manifest` endpoint. Real replay channels carry NO synthetic effects. **Gates: manifest 34/34 (NEW), total 204**; live-verified manifest = z24-replay, all 3 nodes real | ✅ done | D1-6 digital-shadow label + provenance UI |
| 2026-08-14 | **D2-10 (2-day plan) — temperature normalization of the first-mode frequency**: the Z24 first vertical mode wanders ~14% peak-to-peak with air temperature — the #1 false-damage source in vibration SHM. `models/vibration/temperature.py` = simulated seasonal temperature (day-of-year sinusoid peaking 24 Jul, LABELED "not a measured sensor") + thermal f1 model `f1(T) = f1_ref·(1 − α·(T−20))`, α anchored so the full-year swing reproduces the Z24 ~14% shift. The honest quantity is `residual_drift_pct` = measured f1 vs its **thermal** expectation: ~0 when healthy at ANY temperature; negative only when f1 really leaves its thermal band. **Calibration fix (caught live)**: a tracker self-baseline measured in winter already sits ~10% above the 20 C value, so comparing it to a winter-thermal expectation produced a false −6.7% "loss" on a healthy bridge → added `normalize_to_ref()` (baseline → 20 C equivalent) and the tracker now locks a `f1_ref_thermal`, so healthy residual is ~0 at baseline and stays inside the ±7% Z24 band through the season. `backend/app/sim_clock.py` = the simulated monitoring clock (backend source of truth, ~2 simulated d/s, anchored 11 Nov when Z24 monitoring began) — drives the overlay today and the twin's "simulated day N/365" label (D2-8) later. **Gates: smoke 85, stiffness 26 (+2 thermal-live checks), condition 15, deterioration 31, arc 11, temperature 36 (NEW) = 204 checks**; live-verified: healthy residual within ±7% band, rupture forced-tonal reads positive "above thermal band — forced response, never stiffness gain". Commits `04e67e0` | ✅ done | D1-5 data-realism manifest + synthetic channel models |
| 2026-08-14 | **D1-2 (2-day plan) — Z24 bridge identity + honest stiffness overlay**: identity decision → the hero bridge is the **Z24 14+30+14 m post-tensioned box girder** (supports x=0/14/44/58, f1≈3.8 Hz, matching the physics already in contract). `models/vibration/stiffness.py` = Euler-Bernoulli 3-span FEM (EI calibrated so healthy f1 = 3.80 Hz) + self-calibrating damage inversion (a 3.9 Hz healthy bridge reads 0% damage; relative drift maps to mid-span EI loss) + 2% deadband (traffic wander ≠ stiffness loss) + EI drift clamped ≥ 0 (a forced 4 Hz rupture tonal never reads as "stiffening"). `backend/app/stiffness.py` measures f1 from the mid-span node 7 only (off-midspan channels sit on a ~5.1 Hz higher mode — fixed a spurious 26% healthy "drift"), band [2.5,5.0] Hz, median-21 + ±8% tracking gate, 30-window self-baseline. **Live-path storyboard sync**: ws bridge now subscribes `control/cmd` and replays the current scenario to new clients, so the LIVE path drives the same scenario labels/sensor colours/collapse as the replay — a reload mid-arc no longer shows "SYSTEM NOMINAL" while BHI is RED; overlay hint honestly reads "VIBRATION ANOMALY · forced 4 Hz signature active". **Gates: smoke 85/85 (+2 ws scenario checks), stiffness 24/24, arc 11/11**; browser-verified (live f1_ref ≈ 3.84–3.89 Hz healthy, ei_drift 0, damage 0; rupture flips scenario + hint). Commits `ae735d0` + `6fe7768` | ✅ done | D1-3 regulator condition card from crack index |
| 2026-08-14 | **D1-1 (2-day plan) — demo arc PINNED as a regression gate**: `backend/tests/test_demo_arc.py` forces the deterministic spectral-heuristic floor (`trained_push → 0`) and asserts the full story deterministically on any clone, zero data: baseline GREEN in [75,90] → cv beat drops BHI but stays GREEN → 61-step seeded cross-fade passes through AMBER and never returns to GREEN → load+cv escalation → RED in [20,45] → RED stays stable, no flicker. **11/11 PASS**. `scripts/verify_gate.sh` = smoke (83/83) + arc gate, the pre-push merge gate. Values re-pinned against real data by `scripts/verify_demo_arc.py` (data-dependent, next) | ✅ done | D1-2 bridge-identity decision (30 m box girder vs cable-stayed) + D1-3 condition card |
| 2026-08-13 | **LSTM-AE trained + arc re-verified (no flicker)**: `lstm_ae.pt` (30 epochs, P95 recon threshold 3.0e-7, 4050 real-Z24 healthy windows) now loads in the ensemble → **"VAE/OCSVM · envelope-floor+push + LSTM-AE"**. Measured the trained push on real Z24: healthy ≈ **0.003** (envelope absorbs jitter — no false alarm), real damage ≈ **0.14**, damage + rupture overlay ≈ **0.17** — the trained models genuinely contribute without breaking the arc. Also root-caused + fixed a **GREEN→AMBER→GREEN→RED flicker** at 81–85 s: the demo's tendon-snap impact pulse inflated the floor's baseline RMS ~6000× in one window (8.6e-6 → 5.4e-2), so the real damage signature read as "normal" until tonality overcame it. Fix: gate the healthy-envelope refit on `r_ratio <= 1.5` (`anomaly.py`). **Definitive real run (LSTM loaded, Postgres-verified): GREEN 87 → 76–78 (crack 45 s, cv 0.30) → AMBER 67.5 at 75 s onset → RED 49.8 at ~90 s → RED 33.6 at 110 s (bhi-drop beat) → holds RED; NO flicker; alerts at 45/75/110/140 s; backend smoke 83/83** | ✅ done | 6-min demo rehearsal on the locked timeline ([[Storyboard]]) |
| 2026-08-13 | **Integration verified end-to-end**: Docker infra (MQTT 1883/9001 + Postgres healthy) · backend `run_all.py --demo` boots (real Z24 replay, WS :8765, API :8000) · MQTT pipeline live (22 msgs/6s: accel/bhi/alert/status) · twin LIVE over WS, auto-reconnect REPLAY→LIVE, full story (crack→vib→BHI RED 49.9→critical alert→copilot) · backend smoke 83/83 · models smoke 19/19 · twin `npm run build` ✓ | ✅ done | Rehearse 6-min script on demo timeline |
| 2026-08-13 | Twin ws.ts fix: `ws://127.0.0.1:8765` (avoid localhost→::1 race) + 5s retry loop so REPLAY→LIVE handoff is automatic | ✅ done | — |
| 2026-08-13 | Digital twin (R3F) built + live-verified: parametric Morbi bridge, sensors, SVG fleet map, BHI gauge, alerts, copilot, story controls, collapse arc | ✅ done | — |
| 2026-08-13 | ML models built (vibration VAE/OCSVM + LSTM-AE + MiniRocket + heuristic, CV YOLO + OpenCV fallback, BHI fusion) — all with zero-weights demo fallbacks | ✅ done | Real Z24 training (36h stretch) |
| 2026-08-13 | Backend pipeline built: simulator (Z24 replay + synthetic fallback + damage injector), MQTT, Postgres+memory store, WS bridge, FastAPI, demo driver | ✅ done | — |
| 2026-08-13 | Vault knowledge base created (26 notes) | ✅ done | Start [[Pre-Hackathon-Checklist]] items |
| — | — | — | — |

## 36-h build phase trackers

- Phase 0 Freeze (H0–H2) — verify pre-built assets, freeze stack + hero flow
- Phase 1 Core models + pipeline (H2–H10) — CV fine-tune, VAE+OCSVM, simulator→MQTT→Postgres; **H8 ESP32 gate**
- Phase 2 Twin + integration (H10–H16)
- Phase 3 Integration + first rehearsal (H16–H24)
- Phase 4 FEATURE FREEZE (H24–H32) — metrics, hardening, backup videos; presenter stops coding ~H28
- Phase 5 Demo freeze + dry run (H32–H35.5) — submit 30 min early

Details: [[36h-Build-Plan]].

## Live status

- ✅ Research (12-agent workflow) complete
- ✅ Master build plan written
- ✅ Vault knowledge base curated
- ✅ Backend pipeline, ML models, digital twin built (4-agent build workflow)
- ✅ **End-to-end integration verified** — simulator→MQTT→subscriber→bus→WS/API→twin live; story runs 87 GREEN → RED → alert → copilot
- ✅ Smoke tests green (backend 83/83 · models 19/19); twin `npm run build` clean
- ✅ **Real-data phase complete** — all real datasets (Z24 / CrackSeg9k / HBTA / dacl10k / SDNET2018 / CRACK500), real `crack_seg.pt` (YOLO26s-seg), LSTM-AE trained on real Z24
- ✅ **2-day realism plan COMPLETE** — D1-1..D1-6 + D2-7..D2-12 all done; **8 gates / 291 checks, exit 0**; dry-run verified BHI 87.1 → crack 77.7 → AMBER 54 → RED 33.6, no flicker
- ✅ **Finish phase done** — YC-facing README + re-verified Storyboard; **project complete & demo-ready** (`cd backend && python -m app.run_all --demo` + `cd twin && npm run dev`)
- 🔜 Demo rehearsal per [[Storyboard]] (timeline locked), [[Pre-Hackathon-Checklist]]

Related: [[Team]] · [[Data-Pipeline]] · [[CV-Model]] · [[Vibration-Model]] · [[Digital-Twin]]

---

Status: ✅ core build + integration verified — in demo-rehearsal phase
