# Mosquitto broker config — local only, zero venue-WiFi dependency
listener 1883
allow_anonymous true

# WebSocket listener for the twin fallback path
listener 9001
protocol websockets
allow_anonymous true

log_type information
persistence false
