# Training-only deps for the HBTA per-structure vibration retrain.
# The repo's full backend needs more (fastapi/uvicorn/paho/etc.), but NONE of
# that is required here — this tar only runs prep + train + verify.
numpy>=2.0            # np.reshape/axis semantics used by prep (numpy-2.0-pinned)
scikit-learn>=1.4     # OneClassSVM + StandardScaler
torch>=2.3            # VAE + LSTM-AE (CUDA build on the internal server)
h5py>=3.8             # HBTA data_100Hz.h5
joblib>=1.3           # ocsvm.pkl / scaler.pkl serialization
pandas>=2.1           # (optional; h5py direct access used, kept for convenience)
