NORTH STAR: A temperature-aware, physics-grounded "digital shadow" that catches damage the way Norway's Stavå bridge did: the measured natural frequency → a temperature-normalized stiffness proxy (EI drift per span) → an explainable vib trend, fused with real crack-index condition states and modeled (not made-up) traffic, all feeding the BHI with visible uncertainty bands, honestly labeled as a ONE-WAY digital shadow (CDBB maturity ladder), and gated by the verified 87→67.5→33.6 arc as an acceptance test. Every number on screen must trace to a repo dataset (HBTA, CrackSeg9k, live MQTT) or to a clearly-labeled model assumption — that traceability is the realism.

[1] rank=1 effort=M  Temperature/environmental normalization of the vibration signature (the Z24/Tamar lesson)
    WHY: Across all 9 sweeps this is the single biggest realism differentiator in vibration SHM: Z24's lowest mode shifted ~14% over a year and was DOMINATED by temperature, not damage; Tamar removed the thermal common trend with regression/cointegration before alarming. Today our LSTM-AE vib score (anomaly.py) has no defensible baseline against a hot afternoon vs real damage — without this the demo looks naive to any engineer who knows Z24.
    BUILD: Add a synthesized temperature channel (diurnal+seasonal sine with deck gradient lag, from the data-finding recipe) to the live feed; run FFT peak-picking on the accel stream (backend/app/anomaly.py + twin/src/lib/fft.ts already do FFT) to get f1; regress f1 vs T, compute the residual, and surface residual-drift as an EXPLAINABILITY OVERLAY on the vib sub-index — do NOT replace the calibrated LSTM-AE score so the verified 87→67.5→33.6 arc in demo_driver.py stays intact. Show an f(T) scatter + residual trend chart in HealthPanel. Document the Z24 numbers in the vault.
    EVIDENCE: Z24 benchmark (KU Leuven; ~14% lowest-mode seasonal shift, Peeters & De Roeck 2001), Tamar Bridge cointegration (Cross et al. 2013 MSSP), Sydney Harbour thermal coupling (steel arch grows ~10 cm), data-finding temperature-correction section.
    MAPPING: vib sub-index (0.35 BHI weight); streaming: new MQTT topic shm/env/temp; frontend: residual/thermal chart in twin/src/panels/HealthPanel.tsx; BHI honesty (suppresses false AMBER/RED from thermal drift).

[2] rank=2 effort=S  Regulator-style condition card from the real crack index (NBI 0-9 / IQOA 1-3U)
    WHY: Agencies present a condition class, never a raw CV score — France IQOA 1-3U, Germany Zustandsnote 1.0-4.0, Sweden BaTMan condition classes, US NBI 0-9. Mapping our trained CrackSeg9k output onto an inspection-scale rating is the single cheapest way to look credible to a regulator (called out verbatim in the europe sweep).
    BUILD: In the crack pipeline (models/cv/inference.py): from the segmentation mask compute crack area ratio + max width via skeleton+distance transform; map to severity classes (hairline <0.3mm / medium 0.3-1mm / wide >1mm per FHWA/ACI 224R) and to per-component NBI 0-9 (deck/super/substructure), Good/Fair/Poor bucket, and a 'structurally deficient' flag at rating ≤4. Render a condition card on the twin. The crack-index stays the BHI driver so contract.py is untouched.
    EVIDENCE: FHWA NBI 0-9 + sufficiency rating, France IQOA 1-3U (CEREMA), Germany DIN 1076/SIB Zustandsnote, Sweden BaTMan CC, FHWA crack-width criteria + ACI 224R.
    MAPPING: cv sub-index (0.40 BHI weight); frontend condition card; vault note defining the mapping so it is auditable.

[3] rank=3 effort=S  Per-channel data-realism manifest + honest synthetic sensor models
    WHY: The HARD RULE ('never claim stronger than the repo can prove') becomes an artifact instead of a vibe: reviewers audit sigma, sampling rate and filter per channel. The data finding gives concrete numbers (accel 100-200 Hz at 0.3-4 mg RMS, strain 50-100 Hz at ~2 microstrain, temp every 10-60 s, wind 10-20 Hz) that make synthetic streams believable because they match datasheets.
    BUILD: Extend live_feed.py/simulator.py so every MQTT channel is y = physics + Gaussian(sigma_datasheet) + AR(1) drift + Poisson spikes + dropout + 16-bit ADC quantization + temperature coefficient; publish a data-realism manifest (JSON, one entry per channel: unit, rate, sigma, filter, synthetic-or-measured) via API and in the vault. Report the manifest in the UI footer.
    EVIDENCE: data finding (Ragam & Sahebraoji 2019; Birnie & Ravasi 2021; Hagn & Grau 2021), Millau channel mix (fibre extensometers + WIM + anemometry), HK WASHMS multi-modal mix.
    MAPPING: all three sub-indices (noise floors change how believable cv/vib/load are); streaming MQTT topics shm/accel/*, shm/strain/*, shm/wind/anemo, traffic/wim; API + vault; directly enforces the HARD RULE.

[4] rank=4 effort=M  Markov deterioration model + Bayesian updating of the condition curve from the measured crack index
    WHY: The degradation centerpiece: real agencies (Pontis/AASHTOWare, DANBRO, FHWA LTBP, state DOTs) evolve element condition states over time via Markov transition matrices. Showing a curve that MOVES — nudged by each measured crack index through a conjugate Dirichlet-multinomial update — and that prints an uncertainty fan is the difference between a static number and a degradation-aware twin.
    BUILD: 4-state condition model (CoRe/SNBI 1-4) with literature transition priors (Cesare 1992, Golabi/Shepard 1997 Pontis); Bayesian posterior update each time the crack index maps to a state; outputs expected time-to-poor-state and a condition-based next-inspection trigger. Label the chart 'illustrative projection using literature-based transition rates, updated by the measured crack index' — never 'validated RUL'. Add the projection fan to HealthPanel as a time series.
    EVIDENCE: Pontis Markovian decision process, Cesare/Santamarina/Turkstra/Vanmarcke 1992, FHWA BHI synthesis FHWA-HRT-15-081, Bayesian updating J. Bridge Eng 2020 (DOI 10.1061/(asce)be.1943-5592.0001530), DANBRO/Danbro+ Denmark.
    MAPPING: cv sub-index → BHI trend history; frontend projection fan + next-inspection card; vault boundary notes (literature priors, not this bridge's fleet history).

[5] rank=5 effort=M  Stiffness-from-frequency physics proxy + precomputed mode shapes in the 3D viewport
    WHY: Turns the vib score from a black-box LSTM into something that 'behaves like the bridge': f1 measured → EI = 4*f1^2*L^4*rho*A/pi^2 per span → stiffness drift %. The physics sweep proves a 30m concrete box girder lands near f1 3-4 Hz (matches Z24) and a 30m steel girder near 0.9 Hz — numbers a reviewer will check. This is the single highest-leverage realism move per the usa sweep ('make the twin behave like the bridge instead of displaying dashboards').
    BUILD: A numpy Euler-Bernoulli/2D-frame FE core (direct stiffness + consistent mass + generalized eigensolver, ~50 elements/span) computed OFFLINE for the demo bridge; export mode shapes to glTF/JSON; in the twin animate vertices as base + phi*A*sin(2*pi*f_measured*t) with a labeled exaggeration factor; compute the EI-proxy as a second vib explainability overlay. Do NOT attempt real-time FEM reanalysis. Reuse twin/src/scene/TwinCanvas.tsx/MorbiBridge.tsx for the animation.
    EVIDENCE: physics finding (closed-form Euler-Bernoulli, S101 calibrated FE, pyOMA2), Z24 modal grid, MIT LISS/MAC sensor placement, Genoa iTwin, visualization finding (mode-shape animation bound to measured frequency, not a canned loop).
    MAPPING: vib sub-index explainability (0.35 weight); frontend 3D mode-shape/vibration animation + spectrum widget; BHI narrative (explainable drops).

[6] rank=6 effort=M  Realistic traffic/load model driving the load sub-index
    WHY: load is currently the only fully scripted sub-index. Real twin practice fuses REAL loads — WIM trucks into strain/response (Cuomo Bridge, Adibfar/Costin WIM-into-BrIM, Millau 14-class piezoelectric WIM). A Poisson truck stream with bimodal GVW (European 5-axle peak ~30-33t near the 40t legal limit) and 10-20% heavy-truck share, driven over influence lines, makes the 0.25*load term defensible and gives cv/vib real excitation events instead of canned plots.
    BUILD: A generator emitting per-truck WIM events (gross weight, FHWA 13-class, speed, lane) over MQTT topic traffic/wim; influence-line response mapped into the simulated strain/accel channels; a design-load sanity check against Eurocode LM1/AASHTO HL-93 shown in the load panel; WIM axle events aligned to strain peaks and accelerometer bursts (multi-modal event alignment).
    EVIDENCE: COST 323 GVW distributions + accuracy classes A(5)/B(7), Eurocode EN 1991-2 LM1, AASHTO LRFD HL-93 (8/32/32 kip, DLA 33%), Millau piezoelectric WIM, Governor Cuomo Bridge WIM+SHM, Adibfar & Costin (Univ. Florida).
    MAPPING: load sub-index (0.25 BHI weight); streaming traffic/wim topic; multi-modal event alignment feeding cv/vib; frontend load panel + truck events.

[7] rank=7 effort=L  Visualization realism bundle: heatmaps with units, sensor staleness, uncertainty bands, geo context, exaggeration honesty
    WHY: Every sweep converged on the same 'looks fake' traps: static/canned 3D, heatmaps without legends, motion without a stated exaggeration factor, never-showing-uncertainty, and a bridge floating in a black void. The anti-fake cues are data-bound animation, visible uncertainty, and sensor-health — exactly what created buy-in on the real I-90 twin.
    BUILD: Per-vertex strain/displacement heatmap with a diverging colormap centered on the design limit + physical-unit legend; deflection exaggeration factor labeled in the UI ('deflection x5000'); sensor glyphs turn GREY when stale (data-age is the #1 realism signal); uncertainty bands on trend charts (partly present in anomaly.py — surface them); georeferenced context (three.js terrain/ground or Cesium) so the bridge sits in the world; keep the streamable asset 20-50 MB. Land in twin/src/scene/TwinCanvas.tsx + panels.
    EVIDENCE: visualization finding (I-90 data-driven engagement, ESMapV component-level binding, statFEM uncertainty, CesiumJS georeferencing), Genoa Morandi-replacement iTwin, Sydney Harbour web dashboards.
    MAPPING: frontend across all three sub-indices; WS/WebSocket streaming; heatmap values come from cv/vib/load so the 3D is data-bound, not decorative; reinforces the honesty rule.

[8] rank=8 effort=M  Event-triggered capture + replay (Honshu-Shikoku pattern)
    WHY: Real twins don't stream everything at full rate forever: Akashi-Kaikyo logs low-rate background and captures full high-rate records only during wind/quake extremes with staff notification. Adopting this makes the anomaly pipeline look like it has operational intent (threshold -> high-rate capture -> incident record) rather than a constant firehose.
    BUILD: In live_feed.py/anomaly.py: keep the low-rate baseline; when the existing LSTM-AE score or a wind threshold trips, trigger high-rate capture + an event record with the stored window segment; surface it as an incident card in AlertsPanel/StoryControls that REPLAYS the segment (replay, not re-scoring — so the demo arc guardrail cannot break).
    EVIDENCE: Honshu-Shikoku Bridge Authority event-triggered monitoring (wind/earthquake), Stavå bridge threshold+alert (SAP/Forbes), UNH Living Bridge automated stress tests, Rio-Antirrio real-time ops.
    MAPPING: vib sub-index; streaming pipeline (background vs triggered rate); AlertsPanel incident replay; safe because replay is read-only relative to the 87→67.5→33.6 acceptance test.

[9] rank=9 effort=S  Maturity-ladder honesty: label the twin a 'digital shadow' + provenance + a 'claims we will not make' list
    WHY: Free realism: the CDBB/Gemini Principles and BASt maturity ladder (digital model -> digital shadow -> digital twin) are exactly what make an agency trust a demo. A one-way data 'digital shadow' is the honest claim at demo scale — claiming full 'digital twin' with actuation would be the dishonest trap every sweep warns about.
    BUILD: A UI footer + vault doc (vault/06-Risk) explicitly labeling the demo a one-way digital shadow, stating which channels are MEASURED (HBTA vibration, CrackSeg9k, live MQTT) vs MODELED (traffic scripted-but-physics-based, temperature synthesized), publishing the data-realism manifest, and carrying a 'claims we will not make' list: no validated RUL, no Paris-law steel-crack forecast, no calibrated fleet Markov, no bidirectional control.
    EVIDENCE: UK CDBB National Digital Twin Programme + Gemini Principles + Digital Twin Hub, BASt digital-shadow framing, IoTwins edge-cloud architecture.
    MAPPING: vault 06-Risk + 03-Architecture; frontend footer; gates the HARD RULE across every number the demo shows.

[10] rank=10 effort=S  Crack severity -> condition state + image-to-3D photo registration
    WHY: Makes cv a spatial damage layer, not just a number: NEC and PWRI register each inspection photo to model coordinates so cracks are queryable ('all wide cracks on the south face of pier 3') and measured in model space. This is the pattern that turns our real segmenter output into an inspection-grade layer on the twin.
    BUILD: Skeleton + distance transform on the existing CrackSeg9k mask -> crack width/area per detection; register detections to component IDs in the 3D scene (dashed 3D polylines + width labels, pulse only NEW detections); feed severity into the condition card from item 2. Keep the raw image + mask in the detail card so provenance is visible.
    EVIDENCE: NEC bridge digital twin (image AI -> 3D registration), PWRI/NILIM SfM damage annotations in model coordinates, DTU image-based twin, FHWA/ACI 224R width criteria.
    MAPPING: cv sub-index; frontend crack overlay on MorbiBridge + SensorPopup detail; BHI explainability (which crack moved the state).

[11] rank=11 effort=M  Component asset registry (asset-object model)
    WHY: Sund & Bælt and Digital Twin Ontario treat the twin as 'single source of truth' where every component carries its history, specs, manuals and upcoming maintenance — an asset-management UI, not a 3D picture. The FHWA/George Mason ontology reinforces separating physical-domain objects from digital-domain signals.
    BUILD: A JSON asset registry keyed by component IDs that MATCH the 3D scene and sensor schema (contract.py / twin scene); per-component fields: last severity, inspection log, next-inspection date (from item 4), spec, linked crack photos; served via API and shown in raycast-picking detail cards in the twin.
    EVIDENCE: Sund & Bælt (Oresund Landanlaeg, ~2% opex reduction), Digital Twin Ontario, Nye Veier + Brutus BMS, FHWA/George Mason holistic digital-twin ontology.
    MAPPING: backend asset API; frontend picking/detail cards; unifies sensor schema + 3D component IDs + crack annotations into one data spine.

[12] rank=12 effort=S  Seeded-defect / incident replay demo mode grounded in Z24/S101 damage logic
    WHY: The demo arc is currently scripted; re-grounding it in Z24/S101 damage-injection evidence (e.g., pier settlements 20-95mm -> modal-frequency shifts, stiffness-loss -> ~3% f1 drop) makes the narrative evidence-based rather than fabricated, and it is exactly the Stavå arc (twin catches a seeded defect, alarm localized on the 3D model) that the row sweep calls 'the most credible demo narrative'.
    BUILD: A 'damage scenario' mode that reduces per-span EI by N% in the stiffness-proxy overlay (item 5) and shifts f1 accordingly, driving the vib explainability layer; keep the calibrated 87→67.5→33.6 arc as the acceptance test; document in the vault which numbers come from Z24/S101 so reviewers can verify.
    EVIDENCE: Z24 progressive damage (KU Leuven), S101 bridge damage scenarios, Stavå bridge closure detected by its digital twin (S&IE 2024, Forbes/SAP).
    MAPPING: vib sub-index + demo_driver.py; StoryControls incident replay; the verified arc stays as guardrail, not broken by new layers.

